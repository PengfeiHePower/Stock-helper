# Conda environment for stock-helper
# Create:  conda env create -f environment.yml
# Update:  conda env update -f environment.yml --prune
name: stock
channels:
  - conda-forge
dependencies:
  - python=3.12
  - pip
  - pip:
      - -e .
